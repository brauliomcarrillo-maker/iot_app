import 'package:flutter/material.dart';

class OnScreen extends StatelessWidget {
  const OnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Dispositivos activos')));
  }
}
